import { LocalDirective } from './local.directive';

describe('LocalDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalDirective();
    expect(directive).toBeTruthy();
  });
});
